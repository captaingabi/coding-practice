[project]
name = "coding-practice"

[tool.flake8]
max-line-length = 140
exclude = []
ignore = [
    "W503",
    "W504",
]

[[tool.mypy.overrides]]
module = [
    "pytest.*",
    "requests.*",
    "twisted.*",
]
ignore_missing_imports = true
